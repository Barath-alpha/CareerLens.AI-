/* Auth page specific JS — additional utilities */
// Password strength utility (also in main.js, kept for standalone auth pages)
document.addEventListener('DOMContentLoaded', () => {
  if (typeof lucide !== 'undefined') lucide.createIcons();
});
